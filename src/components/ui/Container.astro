---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props as Props;
---

<div
    class={`relative z-10 w-full md:max-w-2xl lg:max-w-230 flex flex-col gap-5 md:gap-6 ${className}`}
>
    <slot />
</div>
